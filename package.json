{
  "name": "profil-felix-fleischmann",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && (cd dist && git init && git checkout -B gh-pages && git add -A && git commit -m 'Deploy static build' && git remote add origin git@github.com:CyBikeLix/felix-fleischmann.git && git push origin gh-pages --force && rm -rf .git)"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.2",
    "@types/leaflet": "^1.9.22",
    "autoprefixer": "^10.4.17",
    "postcss": "^8.4.35",
    "svelte": "^4.2.12",
    "tailwindcss": "^3.4.1",
    "vite": "^5.1.4"
  },
  "dependencies": {
    "chart.js": "^4.4.1",
    "chartjs-plugin-annotation": "^3.1.0",
    "chartjs-plugin-zoom": "^2.2.0",
    "hammerjs": "^2.0.8",
    "leaflet": "^1.9.4",
    "puppeteer": "^24.43.1",
    "svelte-chartjs": "^3.1.5"
  }
}
